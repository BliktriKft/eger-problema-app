FIGMA FILE — CREATED (manual bootstrap)
=========================================

URL:      https://www.figma.com/design/GChYxGM3GpVtYblEosg3FQ/Eger-Varos-Problema-Terkep
File key: GChYxGM3GpVtYblEosg3FQ
Status:   EMPTY (13 pages to be populated by the designer agent via Task D2.5)

BOOTSTRAP STEPS COMPLETED
-------------------------

[x] User created the Figma file manually via the Figma UI on 2026-09-01
[x] File URL + key provided: https://www.figma.com/design/GChYxGM3GpVtYblEosg3FQ/Eger-Varos-Problema-Terkep
[x] File key written into the designer profile .env: FIGMA_FILE_KEY=GChYxGM3GpVtYblEosg3FQ
[ ] 13 pages populated by the designer agent (Task D2.5, deferred until quota allows)

NEXT STEPS (Task D2.5 — DEFERRED)
---------------------------------

When LLM quota allows, trigger the website-designer agent with the plan:

  1. Branch: design/0002-figma-populate-pages
  2. Read design/figma-spec.md for the per-page content spec.
  3. Use the existing PAT (FIGMA_PERSONAL_ACCESS_TOKEN) to call:
       POST https://api.figma.com/v1/files/{file_key}/pages
     for each of the 13 required pages:
       - Cover
       - Design tokens
       - Components (Button, Card, Dialog, Form, VoteButtons, MapMarker,
         Empty/Loading/Error states)
       - Web screens (Landing, Map, Problems list, Problem detail, Submit,
         Auth login/register, Profile)
       - Mobile screens (iOS + Android variants of Map, Feed, Submit,
         Problem detail, Auth)
       - Accessibility audit
  4. For each page, create the documented frames using Figma primitives
     (rectangles, text, frames) that match the design tokens exported to
     apps/web/tailwind.config.ts and apps/mobile/src/theme/tokens.ts.
  5. Commit: 'design: populate 13 Figma pages from design/tokens + figma-spec'.
  6. Push + PR + self-review + merge into main.
  7. The figma-url.txt URL stays canonical.

DESIGN SOURCE OF TRUTH
----------------------

- design/tokens/*.json          — machine-readable tokens (colors, type, spacing, etc.)
- design/components/*.md        — 9 component specs (props, states, a11y)
- design/map/*.svg + pins.tsx   — map pin icon source
- design/accessibility-audit.md — WCAG AA matrix (33/33 pairs)
- design/figma-spec.md          — per-page content spec for the Figma file